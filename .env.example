# ── ME Engineering Assistant — Docker Configuration ──

# Ollama LLM
OLLAMA_MODEL=mistral:7b

# Resource limits (adjust for your machine)
OLLAMA_MEMORY_LIMIT=4g
RAG_MEMORY_LIMIT=2g
UI_MEMORY_LIMIT=1g
NGINX_MEMORY_LIMIT=128m

# Exposed port (only Nginx is exposed to the host)
NGINX_PORT=80
